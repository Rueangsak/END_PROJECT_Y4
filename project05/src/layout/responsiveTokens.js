/** Re-export layout tokens from design system (single source of truth). */
export {
  layoutSpacing,
  containerMaxWidth,
  slideAspectRatio,
  touchTargetMin,
  formMaxWidth,
} from '../design-system/tokens/spacing';
