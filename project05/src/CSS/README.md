# Legacy CSS (removed from build)

Styles previously in this folder were **deleted** during the MUI migration.

- Use `src/design-system/theme/createAppTheme.js` for tokens
- Use `src/design-system/primitives/*` and MUI `sx` for component styles
- Use `src/layout/*` for page shells
- Global resets only: `src/styles/global.css`

Do not reintroduce global CSS files here.
